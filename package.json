{
  "name": "quarto-git-sandbox",
  "version": "1.1.0",
  "private": true,
  "description": "Development dependencies for the quarto-git-sandbox extension. The extension itself ships as plain files and needs no build step to use.",
  "license": "MIT",
  "scripts": {
    "build": "./build.sh",
    "build:vendor": "./build.sh --vendor",
    "test": "./tests/run.sh"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "esbuild": "^0.24.0",
    "isomorphic-git": "^1.41.5",
    "jsdom": "^25.0.0"
  }
}
